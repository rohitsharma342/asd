name: asd_restaurant
description: A digital presence app for your restaurant
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  google_fonts: ^6.1.0
  intl: ^0.18.1
  cached_network_image: ^3.3.1
  flutter_animate: ^4.3.0
  shared_preferences: ^2.2.2
  shared_preferences_web: ^2.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true